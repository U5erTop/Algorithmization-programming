# C# Задания: Понимание работы событий и паттерна MVP (100 заданий + решения)

## 📚 Полный список 100 заданий с решениями 5 случайных задач

---

## Раздел 1: Понимать работу событий (Задания 1-50)

Создание, подписка, отписка на события, EventHandler, пользовательские делегаты.

### Задания 1-50:

1. Создайте простое событие на основе делегата EventHandler
2. Реализуйте класс с событием для оповещения об изменении данных
3. Создайте событие с пользовательским делегатом
4. Реализуйте подписку и отписку на событие
5. Создайте класс Observable с событием OnPropertyChanged
6. Реализуйте событие, которое передает данные через EventArgs
7. Создайте систему уведомлений на основе событий
8. Реализуйте событие для кнопки с параметром клика
9. Создайте событие для отправки email при срабатывании
10. Реализуйте цепочку обработчиков событий
11. Создайте событие для логирования операций
12. Реализуйте событие с обработкой исключений
13. Создайте таймер с событием срабатывания
14. Реализуйте событие для синхронизации данных
15. Создайте событие для начала и конца процесса
16. Реализуйте событие для изменения состояния приложения
17. Создайте событие для обновления UI элементов
18. Реализуйте событие для отслеживания изменения коллекции
19. Создайте событие для обработки пользовательского ввода
20. Реализуйте событие для работы с базой данных
21. Создайте событие для отслеживания файловой системы
22. Реализуйте событие для обработки ошибок в приложении
23. Создайте событие для оповещения о прогрессе операции
24. Реализуйте событие для синхронизации потоков
25. Создайте событие для отслеживания сетевых операций
26. Реализуйте событие для работы с кешем
27. Создайте событие для отправки уведомлений
28. Реализуйте событие для изменения настроек приложения
29. Создайте событие для работы с платежами
30. Реализуйте событие для аудита операций
31. Создайте событие OnClick для пользовательского контрола
32. Реализуйте событие для обработки ввода клавиатуры
33. Создайте событие для работы с валидацией данных
34. Реализуйте событие для отслеживания производительности
35. Создайте событие для работы с API запросами
36. Реализуйте событие для изменения видимости элемента
37. Создайте событие для работы с правами доступа
38. Реализуйте событие для отслеживания активности пользователя
39. Создайте событие для работы с кэшированием результатов
40. Реализуйте событие для оповещения об окончании асинхронной операции
41. Создайте событие для работы с сортировкой и фильтрацией
42. Реализуйте событие для отслеживания состояния сессии
43. Создайте событие для работы с уведомлениями в реальном времени
44. Реализуйте событие для отслеживания изменений в конфигурации
45. Создайте событие для работы с историей операций
46. Реализуйте событие для отправки аналитических данных
47. Создайте событие для работы с шаблонами документов
48. Реализуйте событие для отслеживания состояния подключения
49. Создайте событие для работы с интеграцией сервисов
50. Реализуйте событие для отслеживания фаз жизненного цикла приложения

---

## Раздел 2: Понимать работу паттерна MVP (Задания 51-100)

Архитектура Model-View-Presenter, разделение ответственности, взаимодействие слоев.

### Задания 51-100:

51. Создайте базовую MVP архитектуру с View, Presenter, Model
52. Реализуйте View интерфейс для отделения представления от логики
53. Создайте Presenter класс для управления логикой приложения
54. Реализуйте Model для хранения и обработки данных
55. Создайте MVP для примера калькулятора
56. Реализуйте MVP для управления списком пользователей
57. Создайте MVP с двусторонней привязкой данных
58. Реализуйте MVP для формы регистрации
59. Создайте MVP для отображения таблицы с данными
60. Реализуйте MVP для работы с шопингом-листом
61. Создайте MVP для приложения управления заметками
62. Реализуйте MVP для работы с фильтрацией и сортировкой
63. Создайте MVP для приложения управления контактами
64. Реализуйте MVP с использованием событий для взаимодействия
65. Создайте MVP для отображения и редактирования профиля пользователя
66. Реализуйте MVP для работы с настройками приложения
67. Создайте MVP для навигации между экранами
68. Реализуйте MVP для работы с поиском и фильтрацией
69. Создайте MVP для приложения управления проектами
70. Реализуйте MVP для работы с корзиной покупок
71. Создайте MVP для отображения новостей
72. Реализуйте MVP для работы с чатом
73. Создайте MVP для приложения управления расходами
74. Реализуйте MVP для работы с галереей изображений
75. Создайте MVP для приложения управления задачами
76. Реализуйте MVP для работы с аутентификацией
77. Создайте MVP для отображения графиков и статистики
78. Реализуйте MVP для работы с рецептами
79. Создайте MVP для приложения планирования
80. Реализуйте MVP для работы с букингом
81. Создайте MVP для отображения меню ресторана
82. Реализуйте MVP для работы с ивентами
83. Создайте MVP для приложения рейтингов фильмов
84. Реализуйте MVP для работы с переводом денег
85. Создайте MVP для отображения погоды
86. Реализуйте MVP для работы с маршрутами
87. Создайте MVP для приложения фитнес-трекер
88. Реализуйте MVP для работы с медиа-плеером
89. Создайте MVP для приложения переводчика
90. Реализуйте MVP для работы с календарем
91. Создайте MVP с инъекцией зависимостей (Dependency Injection)
92. Реализуйте MVP с использованием Repository паттерна
93. Создайте MVP для работы с асинхронными операциями
94. Реализуйте MVP с кешированием данных
95. Создайте MVP для работы с множественными источниками данных
96. Реализуйте MVP с тестированием Presenter'а
97. Создайте MVP для работы с валидацией данных
98. Реализуйте MVP с обработкой ошибок
99. Создайте MVP для работы с пагинацией данных
100. Реализуйте MVP с загрузкой данных в фоне

---

# ✅ РЕШЕНИЯ 5 СЛУЧАЙНЫХ ЗАДАНИЙ

## Задание 5: Создайте класс Observable с событием OnPropertyChanged

```csharp
using System;
using System.Collections.Generic;
using System.ComponentModel;

namespace ObservableDemo
{
    // Интерфейс для Observable объектов
    public interface INotifyPropertyChanged
    {
        event PropertyChangedEventHandler PropertyChanged;
    }
    
    // Базовый класс Observable
    public abstract class Observable : INotifyPropertyChanged
    {
        public event PropertyChangedEventHandler PropertyChanged;
        
        // Метод для вызова события при изменении свойства
        protected void OnPropertyChanged(string propertyName)
        {
            PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(propertyName));
            Console.WriteLine($"  [PropertyChanged] {GetType().Name}.{propertyName}");
        }
        
        // Универсальный метод для установки значения с проверкой
        protected bool SetProperty<T>(ref T field, T value, string propertyName)
        {
            if (EqualityComparer<T>.Default.Equals(field, value))
                return false;
            
            field = value;
            OnPropertyChanged(propertyName);
            return true;
        }
    }
    
    // Класс Person наследует Observable
    public class Person : Observable
    {
        private string _name;
        private int _age;
        private string _email;
        
        public string Name
        {
            get => _name;
            set => SetProperty(ref _name, value, nameof(Name));
        }
        
        public int Age
        {
            get => _age;
            set => SetProperty(ref _age, value, nameof(Age));
        }
        
        public string Email
        {
            get => _email;
            set => SetProperty(ref _email, value, nameof(Email));
        }
        
        public override string ToString() => $"{Name}, Age: {Age}, Email: {Email}";
    }
    
    // Класс Product наследует Observable
    public class Product : Observable
    {
        private string _name;
        private decimal _price;
        private int _quantity;
        
        public string Name
        {
            get => _name;
            set => SetProperty(ref _name, value, nameof(Name));
        }
        
        public decimal Price
        {
            get => _price;
            set => SetProperty(ref _price, value, nameof(Price));
        }
        
        public int Quantity
        {
            get => _quantity;
            set => SetProperty(ref _quantity, value, nameof(Quantity));
        }
        
        public override string ToString() => $"{Name} - ${Price:F2} (Qty: {Quantity})";
    }
    
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 5: Observable с OnPropertyChanged ===\n");
            
            // 1. Observable Person
            Console.WriteLine("--- Observable Person ---");
            var person = new Person();
            
            // Подписываемся на изменения свойств
            person.PropertyChanged += (sender, e) =>
            {
                Console.WriteLine($"    → UI обновлена: {e.PropertyName}");
            };
            
            Console.WriteLine("Изменение свойств:");
            person.Name = "Alice";
            person.Age = 25;
            person.Email = "alice@example.com";
            
            Console.WriteLine($"\nТекущие данные: {person}\n");
            
            // 2. Observable Product
            Console.WriteLine("--- Observable Product ---");
            var product = new Product();
            
            // Подписываемся с фильтрацией
            product.PropertyChanged += (sender, e) =>
            {
                if (e.PropertyName == nameof(Product.Price))
                    Console.WriteLine($"    → ⚠️ ЦЕНА ИЗМЕНИЛАСЬ: {e.PropertyName}");
                else
                    Console.WriteLine($"    → ℹ️ Изменение: {e.PropertyName}");
            };
            
            Console.WriteLine("Изменение свойств товара:");
            product.Name = "Laptop";
            product.Price = 999.99m;
            product.Quantity = 5;
            product.Price = 899.99m; // Изменение цены
            
            Console.WriteLine($"\nТекущий товар: {product}\n");
            
            // 3. Множественные подписчики
            Console.WriteLine("--- Множественные подписчики ---");
            var person2 = new Person();
            
            // Подписчик 1: Логирование
            person2.PropertyChanged += (sender, e) =>
            {
                Console.WriteLine($"    [LOG] {e.PropertyName} изменился");
            };
            
            // Подписчик 2: Уведомление
            person2.PropertyChanged += (sender, e) =>
            {
                Console.WriteLine($"    [NOTIFY] Отправка уведомления о {e.PropertyName}");
            };
            
            // Подписчик 3: Аудит
            person2.PropertyChanged += (sender, e) =>
            {
                Console.WriteLine($"    [AUDIT] Записан аудит для {e.PropertyName} в {DateTime.Now:HH:mm:ss}");
            };
            
            Console.WriteLine("Изменение свойства с тремя подписчиками:");
            person2.Name = "Bob";
            
            // 4. Проверка что одинаковые значения не вызывают событие
            Console.WriteLine("\n--- Проверка одинаковых значений ---");
            var person3 = new Person { Name = "Charlie", Age = 30 };
            
            int changeCount = 0;
            person3.PropertyChanged += (sender, e) =>
            {
                changeCount++;
                Console.WriteLine($"    Event #{changeCount}: {e.PropertyName}");
            };
            
            Console.WriteLine("Присваивание того же значения:");
            person3.Name = "Charlie"; // Не должно быть события
            person3.Age = 30; // Не должно быть события
            
            Console.WriteLine($"Количество событий: {changeCount} (должно быть 0)");
            
            Console.WriteLine("\nПрисваивание новых значений:");
            person3.Name = "Charles"; // Должно быть событие
            person3.Age = 31; // Должно быть событие
            
            Console.WriteLine($"Количество событий: {changeCount} (должно быть 2)");
        }
    }
}
```

---

## Задание 9: Создайте событие для отправки email при срабатывании

```csharp
using System;
using System.Collections.Generic;

namespace EmailEventDemo
{
    // Пользовательский класс EventArgs для email событий
    public class EmailEventArgs : EventArgs
    {
        public string To { get; set; }
        public string Subject { get; set; }
        public string Body { get; set; }
        public DateTime Timestamp { get; set; }
        
        public EmailEventArgs(string to, string subject, string body)
        {
            To = to;
            Subject = subject;
            Body = body;
            Timestamp = DateTime.Now;
        }
    }
    
    // Делегат для email события
    public delegate void EmailEventHandler(object sender, EmailEventArgs e);
    
    // Класс User с событиями
    public class User
    {
        public string Name { get; set; }
        public string Email { get; set; }
        
        // События
        public event EmailEventHandler OnAccountCreated;
        public event EmailEventHandler OnPasswordChanged;
        public event EmailEventHandler OnProfileUpdated;
        public event EmailEventHandler OnNotification;
        
        // Создание пользователя
        public User(string name, string email)
        {
            Name = name;
            Email = email;
            
            // Вызываем событие создания аккаунта
            RaiseAccountCreated($"Welcome {name}!", $"Your account has been created. Welcome to our service!");
        }
        
        // Изменение пароля
        public void ChangePassword(string newPassword)
        {
            Console.WriteLine($"\n[USER] Changing password for {Name}...");
            // Имитируем изменение пароля
            RaisePasswordChanged("Password Changed", $"Your password has been successfully changed on {DateTime.Now}");
        }
        
        // Обновление профиля
        public void UpdateProfile(string newName)
        {
            Console.WriteLine($"\n[USER] Updating profile...");
            Name = newName;
            RaiseProfileUpdated("Profile Updated", $"Your profile has been updated: {newName}");
        }
        
        // Отправка уведомления
        public void SendNotification(string message)
        {
            Console.WriteLine($"\n[USER] Sending notification...");
            RaiseNotification("New Notification", message);
        }
        
        // Методы для вызова событий
        private void RaiseAccountCreated(string subject, string body)
        {
            OnAccountCreated?.Invoke(this, new EmailEventArgs(Email, subject, body));
        }
        
        private void RaisePasswordChanged(string subject, string body)
        {
            OnPasswordChanged?.Invoke(this, new EmailEventArgs(Email, subject, body));
        }
        
        private void RaiseProfileUpdated(string subject, string body)
        {
            OnProfileUpdated?.Invoke(this, new EmailEventArgs(Email, subject, body));
        }
        
        private void RaiseNotification(string subject, string body)
        {
            OnNotification?.Invoke(this, new EmailEventArgs(Email, subject, body));
        }
    }
    
    // Email сервис
    public class EmailService
    {
        private List<string> _sentEmails = new List<string>();
        
        public void SendEmail(object sender, EmailEventArgs e)
        {
            string message = $"[EMAIL SERVICE] Sending email to {e.To}\n" +
                           $"  Subject: {e.Subject}\n" +
                           $"  Body: {e.Body}\n" +
                           $"  Time: {e.Timestamp:yyyy-MM-dd HH:mm:ss}";
            Console.WriteLine(message);
            _sentEmails.Add($"{e.To}: {e.Subject}");
        }
        
        public void PrintSentEmails()
        {
            Console.WriteLine("\n=== Sent Emails ===");
            for (int i = 0; i < _sentEmails.Count; i++)
            {
                Console.WriteLine($"{i + 1}. {_sentEmails[i]}");
            }
        }
    }
    
    // SMS сервис
    public class SMSService
    {
        public void SendSMS(object sender, EmailEventArgs e)
        {
            Console.WriteLine($"[SMS SERVICE] Sending SMS to {e.To}\n" +
                            $"  Message: {e.Subject}");
        }
    }
    
    // Логирование сервис
    public class LoggingService
    {
        private List<string> _logs = new List<string>();
        
        public void LogEvent(object sender, EmailEventArgs e)
        {
            string log = $"[{e.Timestamp:HH:mm:ss}] Email event: {e.Subject} -> {e.To}";
            Console.WriteLine($"[LOGGER] {log}");
            _logs.Add(log);
        }
        
        public void PrintLogs()
        {
            Console.WriteLine("\n=== Event Logs ===");
            foreach (var log in _logs)
            {
                Console.WriteLine(log);
            }
        }
    }
    
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 9: Email Event System ===\n");
            
            // Создаем сервисы
            var emailService = new EmailService();
            var smsService = new SMSService();
            var loggingService = new LoggingService();
            
            // Создаем пользователя
            Console.WriteLine("--- Creating User ---");
            var user = new User("Alice", "alice@example.com");
            
            // Подписываем сервисы на события
            Console.WriteLine("\n--- Subscribing Services ---");
            user.OnAccountCreated += emailService.SendEmail;
            user.OnAccountCreated += smsService.SendSMS;
            user.OnAccountCreated += loggingService.LogEvent;
            
            user.OnPasswordChanged += emailService.SendEmail;
            user.OnPasswordChanged += loggingService.LogEvent;
            
            user.OnProfileUpdated += emailService.SendEmail;
            user.OnProfileUpdated += loggingService.LogEvent;
            
            user.OnNotification += emailService.SendEmail;
            user.OnNotification += smsService.SendSMS;
            user.OnNotification += loggingService.LogEvent;
            
            // Выполняем операции
            Console.WriteLine("\n--- Performing Operations ---");
            user.ChangePassword("newSecurePassword123");
            user.UpdateProfile("Alice Smith");
            user.SendNotification("You have a new message!");
            
            // Выводим результаты
            emailService.PrintSentEmails();
            loggingService.PrintLogs();
        }
    }
}
```

---

## Задание 31: Создайте событие OnClick для пользовательского контрола

```csharp
using System;
using System.Collections.Generic;

namespace CustomControlDemo
{
    // EventArgs для кнопки
    public class ButtonClickEventArgs : EventArgs
    {
        public int ClickCount { get; set; }
        public DateTime ClickTime { get; set; }
        public string ButtonName { get; set; }
        
        public ButtonClickEventArgs(string name, int count)
        {
            ButtonName = name;
            ClickCount = count;
            ClickTime = DateTime.Now;
        }
    }
    
    // Делегат для клика
    public delegate void ButtonClickHandler(object sender, ButtonClickEventArgs e);
    
    // Пользовательский контрол Button
    public class CustomButton
    {
        private int _clickCount = 0;
        public string Name { get; set; }
        public bool IsEnabled { get; set; } = true;
        
        // События
        public event ButtonClickHandler OnClick;
        public event ButtonClickHandler OnDoubleClick;
        public event EventHandler OnDisabled;
        
        public CustomButton(string name)
        {
            Name = name;
        }
        
        // Симуляция клика
        public void Click()
        {
            if (!IsEnabled)
            {
                Console.WriteLine($"[BUTTON] {Name} is disabled");
                return;
            }
            
            _clickCount++;
            Console.WriteLine($"[BUTTON] {Name} clicked");
            OnClick?.Invoke(this, new ButtonClickEventArgs(Name, _clickCount));
            
            // Если двойной клик
            if (_clickCount % 2 == 0)
            {
                OnDoubleClick?.Invoke(this, new ButtonClickEventArgs(Name, _clickCount));
            }
        }
        
        // Отключение кнопки
        public void Disable()
        {
            IsEnabled = false;
            Console.WriteLine($"[BUTTON] {Name} disabled");
            OnDisabled?.Invoke(this, EventArgs.Empty);
        }
        
        public int GetClickCount() => _clickCount;
    }
    
    // UI Form
    public class Form
    {
        private List<CustomButton> _buttons = new List<CustomButton>();
        private int _totalClicks = 0;
        
        public void AddButton(CustomButton button)
        {
            _buttons.Add(button);
            
            // Подписываемся на события кнопки
            button.OnClick += OnButtonClicked;
            button.OnDoubleClick += OnButtonDoubleClicked;
            button.OnDisabled += OnButtonDisabled;
        }
        
        private void OnButtonClicked(object sender, ButtonClickEventArgs e)
        {
            _totalClicks++;
            Console.WriteLine($"  ↳ Form heard: {e.ButtonName} clicked #{e.ClickCount} at {e.ClickTime:HH:mm:ss}");
        }
        
        private void OnButtonDoubleClicked(object sender, ButtonClickEventArgs e)
        {
            Console.WriteLine($"  ↳ Form: Double click detected on {e.ButtonName}!");
        }
        
        private void OnButtonDisabled(object sender, EventArgs e)
        {
            Console.WriteLine($"  ↳ Form: A button has been disabled");
        }
        
        public void ShowStatus()
        {
            Console.WriteLine($"\n[FORM] Total clicks: {_totalClicks}");
            foreach (var button in _buttons)
            {
                Console.WriteLine($"  - {button.Name}: {button.GetClickCount()} clicks, Enabled: {button.IsEnabled}");
            }
        }
    }
    
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 31: Custom Button Control ===\n");
            
            // Создаем форму
            var form = new Form();
            
            // Создаем кнопки
            var okButton = new CustomButton("OK");
            var cancelButton = new CustomButton("Cancel");
            var submitButton = new CustomButton("Submit");
            
            // Добавляем в форму
            form.AddButton(okButton);
            form.AddButton(cancelButton);
            form.AddButton(submitButton);
            
            // Добавляем собственные обработчики
            Console.WriteLine("--- Setting up custom handlers ---");
            
            okButton.OnClick += (sender, e) =>
            {
                Console.WriteLine($"      [CUSTOM] OK button action #{e.ClickCount}");
            };
            
            cancelButton.OnClick += (sender, e) =>
            {
                Console.WriteLine($"      [CUSTOM] Cancel button action");
            };
            
            submitButton.OnDoubleClick += (sender, e) =>
            {
                Console.WriteLine($"      [CUSTOM] Submit form (double click)!");
            };
            
            // Симулируем клики
            Console.WriteLine("\n--- Simulating clicks ---");
            okButton.Click();
            okButton.Click(); // Double click
            cancelButton.Click();
            submitButton.Click();
            submitButton.Click(); // Double click
            
            // Отключаем кнопку
            Console.WriteLine("\n--- Disabling button ---");
            okButton.Disable();
            okButton.Click(); // Не будет работать
            
            // Показываем статус
            form.ShowStatus();
        }
    }
}
```

---

## Задание 33: Создайте событие для работы с валидацией данных

```csharp
using System;
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace ValidationEventDemo
{
    // EventArgs для валидации
    public class ValidationEventArgs : EventArgs
    {
        public string FieldName { get; set; }
        public object Value { get; set; }
        public bool IsValid { get; set; }
        public string ErrorMessage { get; set; }
        public DateTime ValidationTime { get; set; }
        
        public ValidationEventArgs(string field, object value)
        {
            FieldName = field;
            Value = value;
            ValidationTime = DateTime.Now;
        }
    }
    
    // Делегат для валидации
    public delegate void ValidationEventHandler(object sender, ValidationEventArgs e);
    
    // Form с валидацией
    public class RegistrationForm
    {
        private string _username;
        private string _email;
        private string _password;
        
        // События
        public event ValidationEventHandler OnFieldValidated;
        public event ValidationEventHandler OnValidationPassed;
        public event ValidationEventHandler OnValidationFailed;
        
        // Свойства с валидацией
        public string Username
        {
            get => _username;
            set => SetAndValidate(ref _username, value, nameof(Username), ValidateUsername);
        }
        
        public string Email
        {
            get => _email;
            set => SetAndValidate(ref _email, value, nameof(Email), ValidateEmail);
        }
        
        public string Password
        {
            get => _password;
            set => SetAndValidate(ref _password, value, nameof(Password), ValidatePassword);
        }
        
        // Метод для установки значения и валидации
        private void SetAndValidate<T>(ref T field, T value, string fieldName, Func<T, bool> validator)
        {
            field = value;
            var args = new ValidationEventArgs(fieldName, value);
            args.IsValid = validator(value);
            args.ErrorMessage = GetErrorMessage(fieldName, args.IsValid);
            
            OnFieldValidated?.Invoke(this, args);
            
            if (args.IsValid)
                OnValidationPassed?.Invoke(this, args);
            else
                OnValidationFailed?.Invoke(this, args);
        }
        
        // Валидаторы
        private bool ValidateUsername(string value)
        {
            if (string.IsNullOrWhiteSpace(value)) return false;
            if (value.Length < 3 || value.Length > 20) return false;
            return Regex.IsMatch(value, @"^[a-zA-Z0-9_]+$");
        }
        
        private bool ValidateEmail(string value)
        {
            if (string.IsNullOrWhiteSpace(value)) return false;
            return Regex.IsMatch(value, @"^[^\s@]+@[^\s@]+\.[^\s@]+$");
        }
        
        private bool ValidatePassword(string value)
        {
            if (string.IsNullOrWhiteSpace(value)) return false;
            if (value.Length < 8) return false;
            return Regex.IsMatch(value, @"^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)");
        }
        
        private string GetErrorMessage(string field, bool isValid)
        {
            if (isValid) return "Valid";
            
            return field switch
            {
                nameof(Username) => "Username must be 3-20 chars and contain only alphanumeric and underscore",
                nameof(Email) => "Invalid email format",
                nameof(Password) => "Password must be 8+ chars with uppercase, lowercase, and digits",
                _ => "Invalid"
            };
        }
    }
    
    // Validator Service
    public class ValidationLogger
    {
        private List<string> _validationLog = new List<string>();
        
        public void LogValidation(object sender, ValidationEventArgs e)
        {
            string status = e.IsValid ? "✓" : "✗";
            string message = $"[{status}] {e.FieldName}: {e.Value} - {e.ErrorMessage}";
            _validationLog.Add(message);
            Console.WriteLine($"  {message}");
        }
        
        public void PrintLog()
        {
            Console.WriteLine("\n=== Validation Log ===");
            foreach (var entry in _validationLog)
                Console.WriteLine(entry);
        }
    }
    
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 33: Validation Events ===\n");
            
            var form = new RegistrationForm();
            var logger = new ValidationLogger();
            
            // Подписываем логгер
            form.OnFieldValidated += logger.LogValidation;
            
            // Счетчики
            int successCount = 0;
            int failureCount = 0;
            
            form.OnValidationPassed += (sender, e) =>
            {
                successCount++;
                Console.WriteLine($"    → Validation passed for {e.FieldName}");
            };
            
            form.OnValidationFailed += (sender, e) =>
            {
                failureCount++;
                Console.WriteLine($"    → ❌ Validation failed: {e.ErrorMessage}");
            };
            
            // Тестирование
            Console.WriteLine("--- Test 1: Valid data ---");
            form.Username = "john_doe";
            form.Email = "john@example.com";
            form.Password = "SecurePass123";
            
            Console.WriteLine("\n--- Test 2: Invalid username ---");
            form.Username = "ab"; // Too short
            
            Console.WriteLine("\n--- Test 3: Invalid email ---");
            form.Email = "invalid-email";
            
            Console.WriteLine("\n--- Test 4: Invalid password ---");
            form.Password = "weak"; // Too weak
            
            // Статистика
            Console.WriteLine($"\n=== Statistics ===");
            Console.WriteLine($"Passed: {successCount}");
            Console.WriteLine($"Failed: {failureCount}");
            
            logger.PrintLog();
        }
    }
}
```

---

## Задание 83: Создайте MVP для приложения рейтингов фильмов

```csharp
using System;
using System.Collections.Generic;
using System.Linq;

namespace MovieRatingMVPDemo
{
    // ============ MODEL ============
    public class Movie
    {
        public int Id { get; set; }
        public string Title { get; set; }
        public string Genre { get; set; }
        public int Year { get; set; }
        public double Rating { get; set; }
        public int VoteCount { get; set; }
        
        public override string ToString() => $"{Title} ({Year}) - {Rating}/10 [{Genre}]";
    }
    
    public class MovieModel
    {
        private List<Movie> _movies = new List<Movie>
        {
            new Movie { Id = 1, Title = "The Shawshank Redemption", Genre = "Drama", Year = 1994, Rating = 9.3, VoteCount = 2500000 },
            new Movie { Id = 2, Title = "The Dark Knight", Genre = "Action", Year = 2008, Rating = 9.0, VoteCount = 2600000 },
            new Movie { Id = 3, Title = "Inception", Genre = "Sci-Fi", Year = 2010, Rating = 8.8, VoteCount = 2200000 },
            new Movie { Id = 4, Title = "Forrest Gump", Genre = "Drama", Year = 1994, Rating = 8.8, VoteCount = 1800000 },
            new Movie { Id = 5, Title = "The Matrix", Genre = "Sci-Fi", Year = 1999, Rating = 8.7, VoteCount = 1600000 }
        };
        
        public List<Movie> GetAllMovies() => _movies;
        
        public Movie GetMovieById(int id) => _movies.FirstOrDefault(m => m.Id == id);
        
        public void RateMovie(int id, double rating)
        {
            var movie = GetMovieById(id);
            if (movie != null && rating >= 1 && rating <= 10)
            {
                movie.Rating = (movie.Rating * movie.VoteCount + rating) / (movie.VoteCount + 1);
                movie.VoteCount++;
            }
        }
        
        public List<Movie> GetMoviesByGenre(string genre) =>
            _movies.Where(m => m.Genre.Equals(genre, StringComparison.OrdinalIgnoreCase)).ToList();
        
        public List<Movie> GetTopMovies(int count) =>
            _movies.OrderByDescending(m => m.Rating).Take(count).ToList();
    }
    
    // ============ VIEW INTERFACE ============
    public interface IMovieView
    {
        void DisplayMovies(List<Movie> movies);
        void DisplayMovieDetails(Movie movie);
        void ShowMessage(string message);
        void ShowError(string error);
        string GetUserInput();
        void ClearDisplay();
    }
    
    // ============ PRESENTER ============
    public class MoviePresenter
    {
        private readonly IMovieView _view;
        private readonly MovieModel _model;
        
        public MoviePresenter(IMovieView view, MovieModel model)
        {
            _view = view;
            _model = model;
        }
        
        public void LoadAllMovies()
        {
            try
            {
                var movies = _model.GetAllMovies();
                _view.DisplayMovies(movies);
            }
            catch (Exception ex)
            {
                _view.ShowError($"Error loading movies: {ex.Message}");
            }
        }
        
        public void DisplayMovieDetails(int movieId)
        {
            try
            {
                var movie = _model.GetMovieById(movieId);
                if (movie != null)
                    _view.DisplayMovieDetails(movie);
                else
                    _view.ShowError($"Movie with ID {movieId} not found");
            }
            catch (Exception ex)
            {
                _view.ShowError($"Error: {ex.Message}");
            }
        }
        
        public void RateMovie(int movieId, double rating)
        {
            try
            {
                if (rating < 1 || rating > 10)
                {
                    _view.ShowError("Rating must be between 1 and 10");
                    return;
                }
                
                _model.RateMovie(movieId, rating);
                var movie = _model.GetMovieById(movieId);
                _view.ShowMessage($"Movie rated! New rating: {movie.Rating:F1}/10");
            }
            catch (Exception ex)
            {
                _view.ShowError($"Error rating movie: {ex.Message}");
            }
        }
        
        public void FilterByGenre(string genre)
        {
            try
            {
                var movies = _model.GetMoviesByGenre(genre);
                if (movies.Count > 0)
                    _view.DisplayMovies(movies);
                else
                    _view.ShowError($"No movies found in genre: {genre}");
            }
            catch (Exception ex)
            {
                _view.ShowError($"Error filtering: {ex.Message}");
            }
        }
        
        public void ShowTopMovies(int count)
        {
            try
            {
                var movies = _model.GetTopMovies(count);
                _view.DisplayMovies(movies);
            }
            catch (Exception ex)
            {
                _view.ShowError($"Error: {ex.Message}");
            }
        }
    }
    
    // ============ VIEW IMPLEMENTATION ============
    public class ConsoleMovieView : IMovieView
    {
        public void DisplayMovies(List<Movie> movies)
        {
            Console.WriteLine("\n=== Movies ===");
            for (int i = 0; i < movies.Count; i++)
            {
                Console.WriteLine($"{i + 1}. {movies[i]}");
            }
        }
        
        public void DisplayMovieDetails(Movie movie)
        {
            Console.WriteLine("\n=== Movie Details ===");
            Console.WriteLine($"ID: {movie.Id}");
            Console.WriteLine($"Title: {movie.Title}");
            Console.WriteLine($"Genre: {movie.Genre}");
            Console.WriteLine($"Year: {movie.Year}");
            Console.WriteLine($"Rating: {movie.Rating:F1}/10 ({movie.VoteCount} votes)");
        }
        
        public void ShowMessage(string message)
        {
            Console.WriteLine($"\n✓ {message}");
        }
        
        public void ShowError(string error)
        {
            Console.WriteLine($"\n✗ Error: {error}");
        }
        
        public string GetUserInput()
        {
            Console.Write("\n> ");
            return Console.ReadLine();
        }
        
        public void ClearDisplay()
        {
            Console.Clear();
        }
    }
    
    // ============ MAIN ============
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 83: MVP Movie Rating App ===");
            
            // Создаем Model и View
            var model = new MovieModel();
            IMovieView view = new ConsoleMovieView();
            
            // Создаем Presenter
            var presenter = new MoviePresenter(view, model);
            
            // Демонстрация работы
            Console.WriteLine("\n--- All Movies ---");
            presenter.LoadAllMovies();
            
            Console.WriteLine("\n--- Top 3 Movies ---");
            presenter.ShowTopMovies(3);
            
            Console.WriteLine("\n--- Movie Details (ID: 1) ---");
            presenter.DisplayMovieDetails(1);
            
            Console.WriteLine("\n--- Rate Movie ---");
            presenter.RateMovie(1, 9.5);
            presenter.DisplayMovieDetails(1);
            
            Console.WriteLine("\n--- Filter by Genre: Drama ---");
            presenter.FilterByGenre("Drama");
            
            Console.WriteLine("\n--- Filter by Genre: Sci-Fi ---");
            presenter.FilterByGenre("Sci-Fi");
            
            Console.WriteLine("\n--- Error Handling ---");
            presenter.RateMovie(1, 15); // Invalid rating
            presenter.DisplayMovieDetails(999); // Non-existent movie
        }
    }
}
```

---

## 📋 Рекомендации по выполнению

### События
- Используйте `event` ключевое слово для определения событий
- Подписывайтесь через `+=` и отписывайтесь через `-=`
- Создавайте собственные `EventArgs` для передачи данных
- Всегда проверяйте делегат на null перед вызовом: `Event?.Invoke(...)`
- Используйте стандартный паттерн: `EventHandler` с `EventArgs`

### MVP паттерн
- **Model** — данные и бизнес-логика
- **View** — интерфейс для отображения (должна быть пассивной)
- **Presenter** — связывает Model и View, содержит логику представления
- View не должна знать о Model напрямую
- Presenter управляет всеми взаимодействиями
- Используйте интерфейсы для View для тестируемости
- Presenter должен быть легко тестируемым

---

## 🔗 Дополнительные материалы

- [События в C#](https://learn.microsoft.com/ru-ru/dotnet/csharp/programming-guide/events/)
- [EventHandler и EventArgs](https://learn.microsoft.com/ru-ru/dotnet/api/system.eventhandler)
- [MVP паттерн](https://en.wikipedia.org/wiki/Model%E2%80%93view%E2%80%93presenter)
- [INotifyPropertyChanged](https://learn.microsoft.com/ru-ru/dotnet/api/system.componentmodel.inotifypropertychanged)
- [Паттерны архитектуры](https://learn.microsoft.com/ru-ru/dotnet/architecture/modern-web-apps-azure/)

---

**Успешное понимание событий и паттерна MVP в C#! 🚀**