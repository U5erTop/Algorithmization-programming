# C# Задания: Упаковка/Распаковка, DateTime/TimeSpan, Вариантность, Перечисления

## 📚 Полный список 100 заданий с решениями 5 случайных задач

---

## Раздел 1: Упаковка и распаковка (Задания 1-20)

### Задания 1-20:

1. Создайте пример упаковки (boxing) переменной типа int в object и продемонстрируйте потерю производительности
2. Реализуйте распаковку (unboxing) значения из object обратно в int с проверкой типа
3. Создайте метод, который принимает object и безопасно распаковывает его в различные типы значений
4. Продемонстрируйте разницу в производительности между упакованными и неупакованными значениями
5. Реализуйте коллекцию ArrayList с примерами упаковки различных типов значений
6. Создайте метод для подсчета количества операций упаковки при работе с ArrayList
7. Реализуйте сравнение производительности List<int> и ArrayList при добавлении 10000 элементов
8. Создайте пример InvalidCastException при неправильной распаковке
9. Реализуйте метод, который принимает params object[] и обрабатывает упакованные значения
10. Создайте класс, демонстрирующий упаковку структуры в интерфейс
11. Реализуйте метод для определения, является ли object упакованным типом значения
12. Создайте пример упаковки nullable типов (int?, double?)
13. Реализуйте профилировщик для отслеживания операций упаковки в коде
14. Создайте метод с использованием generics для избежания упаковки
15. Реализуйте сравнение двух упакованных значений
16. Создайте пример упаковки enum в object и обратной распаковки
17. Реализуйте метод Clone для структуры через упаковку/распаковку
18. Создайте benchmarking тест для измерения накладных расходов boxing/unboxing
19. Реализуйте кеширование упакованных значений для оптимизации
20. Создайте пример изменения упакованного значения через интерфейс

---

## Раздел 2: Виды упаковки (Задания 21-40)

### Задания 21-40:

21. Продемонстрируйте явную упаковку (explicit boxing) типа int
22. Реализуйте неявную упаковку (implicit boxing) при передаче параметров
23. Создайте пример упаковки при вызове виртуальных методов (ToString, GetHashCode)
24. Реализуйте упаковку при приведении к интерфейсу IComparable
25. Создайте пример упаковки при использовании LINQ с типами значений
26. Реализуйте упаковку при добавлении значений в нетипизированную коллекцию
27. Создайте пример упаковки при использовании reflection
28. Реализуйте упаковку при работе с делегатами принимающими object
29. Создайте пример упаковки в событиях EventArgs
30. Реализуйте упаковку при использовании is и as операторов
31. Создайте пример упаковки при работе с динамическими типами (dynamic)
32. Реализуйте упаковку при сериализации структур
33. Создайте пример упаковки при использовании String.Format с типами значений
34. Реализуйте упаковку при работе с Nullable<T> и преобразовании в object
35. Создайте пример упаковки при использовании метода Equals
36. Реализуйте упаковку при работе с attributes и пользовательскими атрибутами
37. Создайте пример упаковки при использовании MethodInfo.Invoke
38. Реализуйте упаковку при работе с WeakReference
39. Создайте пример упаковки при использовании ConditionalWeakTable
40. Реализуйте сравнение различных видов упаковки по производительности

---

## Раздел 3: Работа со структурами DateTime и TimeSpan (Задания 41-60)

### Задания 41-60:

41. Создайте метод для вычисления возраста человека по дате рождения
42. Реализуйте калькулятор рабочих дней между двумя датами (исключая выходные)
43. Создайте метод для определения количества дней до конца года
44. Реализуйте функцию для форматирования DateTime в различных культурах
45. Создайте метод для вычисления разницы между двумя датами в годах, месяцах и днях
46. Реализуйте парсинг строки в DateTime с обработкой различных форматов
47. Создайте метод для добавления рабочих дней к дате
48. Реализуйте функцию для определения, является ли год високосным
49. Создайте метод для получения первого и последнего дня месяца
50. Реализуйте таймер обратного отсчета с использованием TimeSpan
51. Создайте метод для конвертации UTC времени в локальное и обратно
52. Реализуйте функцию для определения квартала года по дате
53. Создайте метод для сравнения дат с точностью до дня (игнорируя время)
54. Реализуйте секундомер (Stopwatch) с использованием DateTime
55. Создайте метод для вычисления продолжительности между двумя TimeSpan
56. Реализуйте функцию для округления DateTime до ближайшего часа/дня/недели
57. Создайте метод для определения недели года по ISO 8601
58. Реализуйте планировщик задач на основе DateTime и TimeSpan
59. Создайте метод для работы с временными зонами (TimeZoneInfo)
60. Реализуйте класс DateRange для работы с диапазонами дат

---

## Раздел 4: Ковариантность и контрвариантность (Задания 61-80)

### Задания 61-80:

61. Создайте ковариантный интерфейс IProducer<out T> для чтения данных
62. Реализуйте контрвариантный интерфейс IConsumer<in T> для записи данных
63. Создайте пример ковариантности с IEnumerable<T>
64. Реализуйте делегат с ковариантным возвращаемым типом
65. Создайте делегат с контрвариантными параметрами
66. Реализуйте иерархию классов (Animal -> Dog -> Puppy) для демонстрации вариантности
67. Создайте ковариантный метод расширения для IEnumerable
68. Реализуйте контрвариантный компаратор IComparer<in T>
69. Создайте пример ошибки при нарушении правил вариантности
70. Реализуйте generic класс с ковариантным параметром типа
71. Создайте Action<in T> делегат с контрвариантностью
72. Реализуйте Func<out TResult> делегат с ковариантностью
73. Создайте интерфейс репозитория с вариантными параметрами
74. Реализуйте паттерн Factory с ковариантностью
75. Создайте пример вариантности в событиях (EventHandler)
76. Реализуйте контрвариантный visitor pattern
77. Создайте ковариантный интерфейс для работы с коллекциями только для чтения
78. Реализуйте безопасное приведение типов с использованием вариантности
79. Создайте пример комбинирования ковариантности и контрвариантности
80. Реализуйте generic метод с ограничениями для поддержки вариантности

---

## Раздел 5: Рассмотрение перечислений (Задания 81-100)

### Задания 81-100:

81. Создайте enum DaysOfWeek и метод для определения выходного дня
82. Реализуйте enum с явными значениями (Status: None=0, Pending=1, Active=2)
83. Создайте флаговое перечисление [Flags] FilePermissions с операциями OR, AND
84. Реализуйте метод для конвертации enum в строку и обратно
85. Создайте enum с атрибутом [Description] и метод для получения описания
86. Реализуйте extension method для получения всех значений enum
87. Создайте метод для проверки, является ли значение валидным для enum
88. Реализуйте enum OrderStatus с методами для переходов между статусами
89. Создайте generic метод для парсинга строки в любой enum
90. Реализуйте enum с базовым типом byte для экономии памяти
91. Создайте метод для получения следующего значения в enum циклически
92. Реализуйте enum Color с методом получения RGB значений
93. Создайте флаговое перечисление UserRoles (Admin, User, Guest, Moderator)
94. Реализуйте метод для подсчета установленных флагов в enum
95. Создайте enum Priority с методом сравнения приоритетов
96. Реализуйте локализацию значений enum
97. Создайте enum с методом для получения связанной иконки или цвета
98. Реализуйте State Machine на основе enum
99. Создайте метод для получения всех комбинаций флагового enum
100. Реализуйте enum с валидацией и бизнес-логикой через extension methods

---

# ✅ РЕШЕНИЯ 5 СЛУЧАЙНЫХ ЗАДАНИЙ

## Задание 5: Реализуйте коллекцию ArrayList с примерами упаковки различных типов значений

```csharp
using System;
using System.Collections;
using System.Diagnostics;

namespace BoxingDemo
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 5: ArrayList и упаковка ===\n");
            
            // ArrayList - нетипизированная коллекция
            ArrayList arrayList = new ArrayList();
            
            // Добавление различных типов значений (происходит boxing)
            arrayList.Add(42);              // int -> boxing
            arrayList.Add(3.14);            // double -> boxing
            arrayList.Add(true);            // bool -> boxing
            arrayList.Add('A');             // char -> boxing
            arrayList.Add(DateTime.Now);    // DateTime (struct) -> boxing
            arrayList.Add("Hello");         // string - ссылочный тип, boxing НЕ происходит
            
            Console.WriteLine("Содержимое ArrayList:");
            foreach (object item in arrayList)
            {
                Console.WriteLine($"Значение: {item,-30} Тип: {item.GetType().Name}");
            }
            
            // Распаковка (unboxing)
            Console.WriteLine("\n=== Распаковка значений ===");
            int unboxedInt = (int)arrayList[0];
            double unboxedDouble = (double)arrayList[1];
            bool unboxedBool = (bool)arrayList[2];
            
            Console.WriteLine($"int: {unboxedInt}");
            Console.WriteLine($"double: {unboxedDouble}");
            Console.WriteLine($"bool: {unboxedBool}");
            
            // Демонстрация производительности
            Console.WriteLine("\n=== Тест производительности ===");
            Stopwatch sw = new Stopwatch();
            
            // ArrayList с boxing
            sw.Start();
            ArrayList list1 = new ArrayList();
            for (int i = 0; i < 100000; i++)
            {
                list1.Add(i); // boxing на каждой итерации
            }
            sw.Stop();
            Console.WriteLine($"ArrayList (с boxing): {sw.ElapsedMilliseconds} мс");
            
            // List<int> без boxing
            sw.Restart();
            List<int> list2 = new List<int>();
            for (int i = 0; i < 100000; i++)
            {
                list2.Add(i); // boxing не происходит
            }
            sw.Stop();
            Console.WriteLine($"List<int> (без boxing): {sw.ElapsedMilliseconds} мс");
        }
    }
}
```

---

## Задание 9: Реализуйте метод, который принимает params object[] и обрабатывает упакованные значения

```csharp
using System;
using System.Collections.Generic;

namespace ParamsObjectDemo
{
    class Program
    {
        // Метод принимает произвольное количество параметров любого типа
        static void ProcessValues(params object[] values)
        {
            Console.WriteLine($"=== Обработка {values.Length} значений ===\n");
            
            foreach (object value in values)
            {
                // Используем pattern matching для определения типа
                switch (value)
                {
                    case int i:
                        Console.WriteLine($"[INT]    Значение: {i}, Квадрат: {i * i}");
                        break;
                        
                    case double d:
                        Console.WriteLine($"[DOUBLE] Значение: {d:F2}, Округленное: {Math.Round(d)}");
                        break;
                        
                    case bool b:
                        Console.WriteLine($"[BOOL]   Значение: {b}, Инверсия: {!b}");
                        break;
                        
                    case string s:
                        Console.WriteLine($"[STRING] Значение: '{s}', Длина: {s.Length}");
                        break;
                        
                    case DateTime dt:
                        Console.WriteLine($"[DATE]   Значение: {dt:yyyy-MM-dd HH:mm:ss}");
                        break;
                        
                    case null:
                        Console.WriteLine($"[NULL]   Значение: null");
                        break;
                        
                    default:
                        Console.WriteLine($"[OTHER]  Тип: {value.GetType().Name}, Значение: {value}");
                        break;
                }
            }
        }
        
        // Метод для подсчета количества значений каждого типа
        static Dictionary<Type, int> CountTypes(params object[] values)
        {
            var typeCounts = new Dictionary<Type, int>();
            
            foreach (object value in values)
            {
                Type type = value?.GetType() ?? typeof(object);
                
                if (typeCounts.ContainsKey(type))
                    typeCounts[type]++;
                else
                    typeCounts[type] = 1;
            }
            
            return typeCounts;
        }
        
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 9: params object[] ===\n");
            
            // Вызов метода с различными типами (boxing происходит для типов значений)
            ProcessValues(
                42,                      // int -> boxing
                3.14159,                 // double -> boxing
                true,                    // bool -> boxing
                "Hello World",           // string - нет boxing
                DateTime.Now,            // DateTime (struct) -> boxing
                'X',                     // char -> boxing
                null                     // null
            );
            
            Console.WriteLine("\n=== Подсчет типов ===\n");
            var counts = CountTypes(10, 20, 30, "test", "hello", 5.5, 7.7, 8.8, true, false);
            
            foreach (var kvp in counts)
            {
                Console.WriteLine($"{kvp.Key.Name}: {kvp.Value} шт.");
            }
        }
    }
}
```

---

## Задание 31: Создайте пример упаковки при работе с динамическими типами (dynamic)

```csharp
using System;
using System.Diagnostics;

namespace DynamicBoxingDemo
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 31: Dynamic и упаковка ===\n");
            
            // dynamic хранит значения как object, поэтому происходит boxing
            int valueInt = 100;
            dynamic dynamicValue = valueInt;  // boxing int -> object
            
            Console.WriteLine($"Dynamic значение: {dynamicValue}");
            Console.WriteLine($"Тип dynamic: {dynamicValue.GetType()}");
            Console.WriteLine($"Это упакованный int: {dynamicValue.GetType() == typeof(int)}\n");
            
            // Демонстрация различных типов
            dynamic d1 = 42;           // int -> boxing
            dynamic d2 = 3.14;         // double -> boxing
            dynamic d3 = true;         // bool -> boxing
            dynamic d4 = "text";       // string - нет boxing (ссылочный тип)
            dynamic d5 = DateTime.Now; // DateTime -> boxing
            
            Console.WriteLine("=== Типы dynamic переменных ===");
            Console.WriteLine($"d1: {d1.GetType().Name}");
            Console.WriteLine($"d2: {d2.GetType().Name}");
            Console.WriteLine($"d3: {d3.GetType().Name}");
            Console.WriteLine($"d4: {d4.GetType().Name}");
            Console.WriteLine($"d5: {d5.GetType().Name}");
            
            // Операции с dynamic
            Console.WriteLine("\n=== Операции с dynamic ===");
            dynamic result = d1 + d1;  // 42 + 42
            Console.WriteLine($"42 + 42 = {result} (тип: {result.GetType().Name})");
            
            // Изменение типа в runtime
            Console.WriteLine("\n=== Изменение типа dynamic ===");
            dynamic changing = 100;
            Console.WriteLine($"Изначально: {changing} ({changing.GetType().Name})");
            
            changing = "Теперь строка";
            Console.WriteLine($"Теперь: {changing} ({changing.GetType().Name})");
            
            changing = 99.99;
            Console.WriteLine($"И снова: {changing} ({changing.GetType().Name})");
            
            // Проблемы производительности с dynamic
            Console.WriteLine("\n=== Тест производительности ===");
            const int iterations = 1000000;
            
            Stopwatch sw = new Stopwatch();
            
            // Обычный int
            sw.Start();
            int sum1 = 0;
            for (int i = 0; i < iterations; i++)
            {
                sum1 += i;
            }
            sw.Stop();
            Console.WriteLine($"int: {sw.ElapsedMilliseconds} мс");
            
            // dynamic (с boxing)
            sw.Restart();
            dynamic sum2 = 0;
            for (int i = 0; i < iterations; i++)
            {
                sum2 += i;
            }
            sw.Stop();
            Console.WriteLine($"dynamic: {sw.ElapsedMilliseconds} мс");
        }
    }
}
```

---

## Задание 33: Создайте пример упаковки при использовании String.Format с типами значений

```csharp
using System;
using System.Diagnostics;

namespace StringFormatBoxingDemo
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 33: String.Format и упаковка ===\n");
            
            // String.Format принимает params object[], поэтому типы значений упаковываются
            int age = 25;
            double salary = 50000.50;
            bool isActive = true;
            char grade = 'A';
            
            // Boxing происходит для всех типов значений
            string formatted = String.Format(
                "Возраст: {0}, Зарплата: {1:C}, Активен: {2}, Оценка: {3}",
                age,      // int -> boxing
                salary,   // double -> boxing
                isActive, // bool -> boxing
                grade     // char -> boxing
            );
            
            Console.WriteLine(formatted);
            
            // Альтернативы без boxing
            Console.WriteLine("\n=== Альтернативы String.Format ===");
            
            // 1. Интерполяция строк (string interpolation) - более эффективна
            string interpolated = $"Возраст: {age}, Зарплата: {salary:C}, Активен: {isActive}, Оценка: {grade}";
            Console.WriteLine($"Интерполяция: {interpolated}");
            
            // 2. StringBuilder
            var sb = new System.Text.StringBuilder();
            sb.Append("Возраст: ").Append(age)
              .Append(", Зарплата: ").Append(salary)
              .Append(", Активен: ").Append(isActive)
              .Append(", Оценка: ").Append(grade);
            Console.WriteLine($"StringBuilder: {sb}");
            
            // 3. String.Concat - но также происходит boxing
            string concatenated = String.Concat("Возраст: ", age, ", Зарплата: ", salary);
            Console.WriteLine($"Concat: {concatenated}");
            
            // Тест производительности
            Console.WriteLine("\n=== Тест производительности ===");
            const int iterations = 100000;
            Stopwatch sw = new Stopwatch();
            
            // String.Format (с boxing)
            sw.Start();
            for (int i = 0; i < iterations; i++)
            {
                string s = String.Format("Число: {0}, Десятичное: {1}", i, i * 0.5);
            }
            sw.Stop();
            Console.WriteLine($"String.Format: {sw.ElapsedMilliseconds} мс");
            
            // String interpolation (оптимизирован компилятором)
            sw.Restart();
            for (int i = 0; i < iterations; i++)
            {
                string s = $"Число: {i}, Десятичное: {i * 0.5}";
            }
            sw.Stop();
            Console.WriteLine($"Интерполяция: {sw.ElapsedMilliseconds} мс");
            
            // Демонстрация composite formatting
            Console.WriteLine("\n=== Composite Formatting ===");
            Console.WriteLine("Имя: {0}, Возраст: {1}, Рост: {2:F2}м", "Иван", 30, 1.85);
            Console.WriteLine("Цена: {0:C}, Количество: {1}, Итого: {2:C}", 99.99, 5, 99.99 * 5);
        }
    }
}
```

---

## Задание 83: Создайте флаговое перечисление [Flags] FilePermissions с операциями OR, AND

```csharp
using System;

namespace FlagsEnumDemo
{
    // Флаговое перечисление для прав доступа к файлам
    [Flags]
    enum FilePermissions
    {
        None = 0,           // 0000
        Read = 1,           // 0001
        Write = 2,          // 0010
        Execute = 4,        // 0100
        Delete = 8,         // 1000
        
        // Комбинированные права
        ReadWrite = Read | Write,                          // 0011
        ReadExecute = Read | Execute,                      // 0101
        ReadWriteExecute = Read | Write | Execute,         // 0111
        FullControl = Read | Write | Execute | Delete      // 1111
    }
    
    class Program
    {
        // Метод для красивого отображения прав доступа
        static void DisplayPermissions(FilePermissions permissions)
        {
            Console.WriteLine($"\nТекущие права доступа: {permissions}");
            Console.WriteLine($"Двоичное представление: {Convert.ToString((int)permissions, 2).PadLeft(4, '0')}");
            Console.WriteLine("Детали:");
            
            if (permissions.HasFlag(FilePermissions.Read))
                Console.WriteLine("  ✓ Read (Чтение)");
            else
                Console.WriteLine("  ✗ Read (Чтение)");
                
            if (permissions.HasFlag(FilePermissions.Write))
                Console.WriteLine("  ✓ Write (Запись)");
            else
                Console.WriteLine("  ✗ Write (Запись)");
                
            if (permissions.HasFlag(FilePermissions.Execute))
                Console.WriteLine("  ✓ Execute (Выполнение)");
            else
                Console.WriteLine("  ✗ Execute (Выполнение)");
                
            if (permissions.HasFlag(FilePermissions.Delete))
                Console.WriteLine("  ✓ Delete (Удаление)");
            else
                Console.WriteLine("  ✗ Delete (Удаление)");
        }
        
        // Метод для подсчета установленных флагов
        static int CountSetFlags(FilePermissions permissions)
        {
            int count = 0;
            int value = (int)permissions;
            
            while (value > 0)
            {
                if ((value & 1) == 1)
                    count++;
                value >>= 1;
            }
            
            return count;
        }
        
        static void Main()
        {
            Console.WriteLine("=== ЗАДАНИЕ 83: Флаговое перечисление FilePermissions ===");
            
            // 1. Создание прав доступа с помощью OR (|)
            Console.WriteLine("\n=== 1. Комбинирование прав (OR) ===");
            FilePermissions userPermissions = FilePermissions.Read | FilePermissions.Write;
            DisplayPermissions(userPermissions);
            
            // 2. Проверка наличия права с помощью AND (&)
            Console.WriteLine("\n=== 2. Проверка прав (AND) ===");
            bool canRead = (userPermissions & FilePermissions.Read) != 0;
            bool canExecute = (userPermissions & FilePermissions.Execute) != 0;
            
            Console.WriteLine($"Можно читать: {canRead}");
            Console.WriteLine($"Можно выполнять: {canExecute}");
            
            // Альтернативный способ - HasFlag (более читаемый)
            Console.WriteLine($"\nHasFlag(Read): {userPermissions.HasFlag(FilePermissions.Read)}");
            Console.WriteLine($"HasFlag(Execute): {userPermissions.HasFlag(FilePermissions.Execute)}");
            
            // 3. Добавление права
            Console.WriteLine("\n=== 3. Добавление права Execute ===");
            userPermissions |= FilePermissions.Execute;
            DisplayPermissions(userPermissions);
            
            // 4. Удаление права
            Console.WriteLine("\n=== 4. Удаление права Write ===");
            userPermissions &= ~FilePermissions.Write;
            DisplayPermissions(userPermissions);
            
            // 5. Переключение (toggle) права
            Console.WriteLine("\n=== 5. Переключение права Write (XOR) ===");
            userPermissions ^= FilePermissions.Write;
            DisplayPermissions(userPermissions);
            
            Console.WriteLine("\n=== 6. Еще одно переключение Write ===");
            userPermissions ^= FilePermissions.Write;
            DisplayPermissions(userPermissions);
            
            // 7. Установка полного контроля
            Console.WriteLine("\n=== 7. Полный контроль ===");
            userPermissions = FilePermissions.FullControl;
            DisplayPermissions(userPermissions);
            Console.WriteLine($"Количество установленных флагов: {CountSetFlags(userPermissions)}");
            
            // 8. Сброс всех прав
            Console.WriteLine("\n=== 8. Сброс всех прав ===");
            userPermissions = FilePermissions.None;
            DisplayPermissions(userPermissions);
            
            // 9. Примеры использования
            Console.WriteLine("\n=== 9. Практические примеры ===");
            
            FilePermissions adminPermissions = FilePermissions.FullControl;
            FilePermissions guestPermissions = FilePermissions.Read;
            FilePermissions moderatorPermissions = FilePermissions.ReadWrite;
            
            Console.WriteLine($"Админ: {adminPermissions}");
            Console.WriteLine($"Гость: {guestPermissions}");
            Console.WriteLine($"Модератор: {moderatorPermissions}");
            
            // 10. Получение всех возможных прав
            Console.WriteLine("\n=== 10. Все значения перечисления ===");
            foreach (FilePermissions perm in Enum.GetValues(typeof(FilePermissions)))
            {
                Console.WriteLine($"{perm,-20} = {(int)perm,2} (Binary: {Convert.ToString((int)perm, 2).PadLeft(4, '0')})");
            }
        }
    }
}
```

---

## 📋 Рекомендации по выполнению

### Упаковка и распаковка
- Используйте типизированные коллекции (`List<T>`) вместо `ArrayList`
- Избегайте упаковки в критичных по производительности участках кода
- Используйте generics для предотвращения boxing/unboxing
- Всегда проверяйте тип перед распаковкой

### Виды упаковки
- Осознавайте скрытую упаковку при вызове методов интерфейсов
- String interpolation часто эффективнее String.Format
- LINQ с типами значений может приводить к упаковке
- Используйте профилировщики для обнаружения boxing

### DateTime и TimeSpan
- Используйте `DateTime.UtcNow` для работы с UTC
- `TimeSpan` для представления интервалов времени
- `DateTimeOffset` для работы с часовыми поясами
- Методы `Parse` и `TryParse` для безопасного парсинга

### Ковариантность и контрвариантность
- `out` для ковариантности (только возврат значений)
- `in` для контрвариантности (только прием параметров)
- Работает только с ссылочными типами
- Применимо к интерфейсам и делегатам

### Перечисления
- Используйте `[Flags]` для битовых флагов
- Определяйте явные значения степенями двойки (1, 2, 4, 8...)
- `HasFlag()` для проверки флагов
- Extension methods для добавления функциональности

---

## 🔗 Дополнительные материалы

- [Boxing и Unboxing (Microsoft Docs)](https://learn.microsoft.com/ru-ru/dotnet/csharp/programming-guide/types/boxing-and-unboxing)
- [DateTime](https://learn.microsoft.com/ru-ru/dotnet/api/system.datetime)
- [TimeSpan](https://learn.microsoft.com/ru-ru/dotnet/api/system.timespan)
- [Ковариантность и контрвариантность](https://learn.microsoft.com/ru-ru/dotnet/standard/generics/covariance-and-contravariance)
- [Перечисления](https://learn.microsoft.com/ru-ru/dotnet/csharp/language-reference/builtin-types/enum)

---

**Успехов в изучении C#! 🚀**